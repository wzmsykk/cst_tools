# VB Function 
# Double EigenResult_Complex(iModeNumber,queryType,axis,uvw)
# 计算沿某轴积分得EigenResult_Complex的结果
# Integer iModeNumber 代表当前计算的模式,第一个模式设为1
# Integer axis=[1,2,3] 分别代表x,y,z
# Array uvw(3,3) 
# 例 axis取2 (z轴), uvw(2,1)=5, uvw(2,2)=0, uvw(2,3)=0 代表沿z方向 xoffset=5, yoffset=0, stepsize=0(auto) 积分"



# R_over_Q_output
# 
# 输出结果到文件outputPath
# 推荐格式 Mode_${iModeNumber}_R_over_Q_${user_defination}.txt